# Math-Quiz

Operands were being regenerated after being shown to the user due
to incorrect sequencing, so the answers were checked against
different numbers than the question displayed. Operator is now
selected before operands are generated, and generation happens
only once every iteration.
